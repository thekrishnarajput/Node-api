const router = require('router').Router();
const customers = require('./bin/customerController');

/* router.post('/create-customer', async (req, res) => {
    try {
        let Body = req.body;
        
    } catch(error) {
        
    }
})
router.post("/all-customer", customerController.findAllCustomer);
 */

module.exports = router;